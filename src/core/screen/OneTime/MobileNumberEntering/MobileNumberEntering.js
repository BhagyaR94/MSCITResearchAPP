import * as React from 'react';
import {View, Text} from 'react-native';

let MobileNumberEntering = ({ navigation }) => {
    return (
        <View>
            <Text>MOBILE NUMBER ENTERING ACCESSED</Text>
        </View>
    );
};

export default MobileNumberEntering;